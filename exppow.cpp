#include <stdio.h>
#include <iostream>
#include <queue>
#include <vector>

using namespace std;

int BFS(int s){
	int visited[V]={0};
	queue<int> q;
	visited[s]=1;
	queue.push(s);
	while(!q.empty()){
		int x = q.front();
		q.pop();
		
	}
}


int main(){
	int n;

}